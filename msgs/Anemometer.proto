syntax = "proto2";
package gz_sensor_msgs.msgs;

import "Header.proto";
import "vector3d.proto";

// Anemometer message type which is emitted by the
// Anemometer Gazebo plugin
message Anemometer
{
  required gz_std_msgs.Header header = 1;
  required gazebo.msgs.Vector3d velocity = 2;
}
